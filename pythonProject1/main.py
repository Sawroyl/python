#prompt input of list of integers from user
n=input('Enter list of integers with space between each: ')
#split string input to list
nlist=n.split()
list=[]
for n in nlist:
    if i>100:
        list.append(over)
    else:
        list.append(nlist)
print(list)



